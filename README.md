# Multi-Modal App Backend

## Setup

```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt


```cmd
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
